node {
    stage('build') {
        //def myImage = docker.image ('nebiha11/hello')
        //bat " docker run nebiha11/hello"
        def myImage = build(image[dockerfile])
          
    }
}
